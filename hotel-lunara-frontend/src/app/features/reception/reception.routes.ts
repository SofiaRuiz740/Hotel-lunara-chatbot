import { Routes } from '@angular/router';
import { ReceptionPageComponent } from './reception.page';

export const RECEPTION_ROUTES: Routes = [
  {
    path: '',
    component: ReceptionPageComponent,
  },
];
